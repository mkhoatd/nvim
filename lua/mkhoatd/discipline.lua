local M = {}

-- function M.cowboy()
--   ---@type table?
--   local id
--   local ok = true
--
--   for _, key in iparis({ "h", "j", "k", "l", "+", "-" }) do
--     local count = 0
--     local timer = assert(vim.loop.new_timer())
--     vim.keymap.
--   end
-- end
